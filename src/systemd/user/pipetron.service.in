[Unit]
Description=A third-party daemon to fix Electron audio streams naming conflicts in PipeWire.
After=pipewire.service

[Service]
ExecStart=@BINARY_PATH@ -d
Restart=on-failure

[Install]
WantedBy=pipewire.service